hedera:
  mirror:
    importer:
      db:
        host: 127.0.0.1
        port: 5432
        name: mirror_node
        owner: mirror_node
        ownerPassword: mirror_node_pass
        username: mirror_importer
        password: mirror_importer_pass
      parser:
        record:
          entity:
            redis:
              enabled: false
          performance:
            transactions:
              - entities: 1
                tps: 1
                type: CRYPTOTRANSFER
